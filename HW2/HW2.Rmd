---
title: "STAT416 Assignment 2"
author: "Ashish Jain"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
    echo = TRUE,
    message = FALSE,
    warning = FALSE
)
```

#Question 1:

a). The treatment factors considered in the experiment is temperature and genotype.

b). The treatment levels considered in the experiment for temperature are normal, cold and hot and for genotype it is .

c). Expermimental units in this experiment is the house. We have six houses in this experiment consisting of 4 chicken each.

d). The observational unit in this experiment is the chicken.

e). 

f). 

#Question 2:

#Question 3:


#Question 4:

##a) 

i). 
$$
\begin{aligned}
 E(2Y_1 + 3) &\stackrel{ind.}{=} E(2Y_1) + E(3) \\
  &= 2E(Y_1) + 3 \\
  &= 2 \mu_1 + 3 \\
\end{aligned}
$$

ii).
$$
\begin{aligned}
 E(Y_1 + Y_2 + Y_3) &\stackrel{ind.}{=} E(Y_1) + E(Y_2) + E(Y_3) \\
  &= \mu_1 + \mu_2 + \mu_3 \\
\end{aligned}
$$

##b).

i).
$$
\begin{aligned}
 E(c) &= c
\end{aligned}
$$

ii).
$$
\begin{aligned}
 Var(c) &= E[\{c-E(c)\}^2] \\
 &= E[\{c-c\}^2] \\
 &= 0
\end{aligned}
$$

##c).

i).
$$
\begin{aligned}
 Var(Y_1-Y_2) &\stackrel{ind.}{=} Var(Y_1) + Var(Y_2) \\
 &= \sigma^2 + \sigma^2 \\
 &= 2\sigma^2
\end{aligned}
$$

ii).
$$
\begin{aligned}
 Var(\frac{Y_1+Y_2}{2}) &= \frac{Var(Y_1+Y_2)}{4} \\
 &\stackrel{ind.}{=} \frac{Var(Y_1)}{4} + \frac{Var(Y_2)}{4} \\
 &= \frac{\sigma^2}{4} + \frac{\sigma^2}{4} \\
 &= 2*\frac{\sigma^2}{4} \\
 &= \frac{\sigma^2}{2}
\end{aligned}
$$

##d).

i).
$$
\begin{aligned}
 Var(\sum{n}{i=1}Y_i) &\stackrel{ind.}{=} Var(Y_1) + Var(Y_2) + \dots + Var(Y_n) \\
 &= \sigma^2 + \sigma^2 + \dots + \sigma^2\\
 &= n\sigma^2
\end{aligned}
$$

ii).
$$
\begin{aligned}
 Var(\frac{\sum{n}{i=1}Y_i}{n}) &\stackrel{ind.}{=} Var(\frac{(Y_1)}{n}) + Var(\frac{(Y_2)}{n}) + \dots + Var(\frac{(Y_n)}{n})\\
 &= \frac{Var(Y_1)}{n^2} + \frac{Var(Y_2)}{n^2} + \dots + \frac{Var(Y_n)}{n^2} \\
 &= \frac{\sigma^2}{n^2} + \frac{\sigma^2}{n^2} + \dots + \frac{\sigma^2}{n^2} \\
 &= n*\frac{\sigma^2}{n^2} \\
 &= \frac{\sigma^2}{n}
\end{aligned}
$$

##e).
$$
\label{eq:test}
\begin{aligned}
 Var(\bar{Y_{1..}}- \bar{Y_{2..}}) &\stackrel{From Slides}{=} Var(T_1 + T_2 + \bar{b_{1.}} + \bar{b_{2.}} + \bar{e_{1..}} + \bar{e_{2..}}) \\
 &\stackrel{ind.}{=} Var(T_1) + Var(T_2) + Var(\bar{b_{1.}}) + Var(\bar{b_{2.}}) + Var(\bar{e_{1..}}) + Var(\bar{e_{2..}}) \\
 &= 0 + 0 + \frac{{\sigma_b}^2}{n} + \frac{{\sigma_b}^2}{n} + \frac{{\sigma_e}^2}{m} + \frac{{\sigma_e}^2}{m} \\
 &= 2*\frac{{\sigma_b}^2}{n} + 2*\frac{{\sigma_e}^2}{m}
\end{aligned}
$$

i).
In Design 1, n is equal to 8 and m is equal to 8,
$$
\begin{aligned}
 Var(\bar{Y_{1..}}- \bar{Y_{2..}}) &= 2*\frac{{\sigma_b}^2}{n} + 2*\frac{{\sigma_e}^2}{m}
 &= 2*\frac{{\sigma_b}^2}{8} + 2*\frac{{\sigma_e}^2}{8} \\ 
 &= \frac{{\sigma_b}^2}{4} + \frac{{\sigma_e}^2}{4} \\ 
 &= \frac{{\sigma_b}^2+{\sigma_e}^2}{4}
\end{aligned}
$$

ii).

In Design 2, n is equal to 4 and m is equal to 8,
$$
\begin{aligned}
 Var(\bar{Y_{1..}}- \bar{Y_{2..}}) &= 2*\frac{{\sigma_b}^2}{n} + 2*\frac{{\sigma_e}^2}{m}
 &= 2*\frac{{\sigma_b}^2}{4} + 2*\frac{{\sigma_e}^2}{8} \\ 
 &= 2*\frac{{\sigma_b}^2}{4} + \frac{{\sigma_e}^2}{4} \\ 
 &= \frac{2{\sigma_b}^2+{\sigma_e}^2}{4}
\end{aligned}
$$

